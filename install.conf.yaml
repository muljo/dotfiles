- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim: .vim
    ~/.vimrc: .vim/.vimrc

    ~/.bashrc: .bash/.bashrc
    ~/.profile: .bash/.profile

- create:
    - ~/downloads
    - ~/.vim/undo-history
